# 专家意见逐条修改说明

对应修订文件：

- 论文Word稿：`paper/2026-0268-基于多专家融合的铁路客流多尺度预测方法.docx`
- 实验记录：`docs/experiments/RAMR补充实验结果_20260630.md`
- 数据核验：`docs/experiments/数据真实性与实验口径核验_20260630.md`
- 正确数据复现实验：`docs/experiments/正确数据复现实验结果_20260630.md`
- MAE导向补充实验：`docs/experiments/MAE导向多目标补充实验_20260630.md`
- 严格无泄露改进实验：`docs/experiments/严格无泄露改进实验_20260630.md`
- 审稿意见完成度审计：`docs/experiments/审稿意见完成度审计_20260701.md`
- 关键代码：`src/model.py`、`src/train.py`、`src/train_modern_baselines.py`、`src/plot_paper_figures.py`

本轮修订的核心策略是将论文叙事从“多专家模型工程组合”调整为“场景条件化非平稳时序分解-路由机制”。修订稿引入 Regime-aware Mixture Routing（RAMR）表述：通过日历场景、窗口级分布状态和专家功能分工，将趋势、周期、扰动结构映射到短期、长期、分布偏移专家空间，实现有机制解释的 MoE 路由。

## 数据真实性与实验口径说明

本轮重新核验了实验数据来源。论文主实验只使用项目内两份真实南京南站客流 CSV：

- `data/from_nj.csv`：南京南站到其他站点的日粒度客流量。
- `data/to_nj.csv`：其他站点到南京南站的日粒度客流量。

两个文件均为 GBK 编码，原始站点字段为 `北京南`、`成都东`、`广州南`、`汉口`、`杭州东`、`杭州西`、`上海`、`上海虹桥`、`武汉`、`西安北`。代码按 `time` 字段内连接两张表，得到 846 个共同日期和 20 个站点-方向目标变量。`from_nj.csv` 无缺失、无重复日期，SHA256 为 `c4f8919ce83c4c677d772036ed42fb5e76c99507f3368e2d32f081b556df4ae7`；`to_nj.csv` 无缺失、无重复日期，SHA256 为 `d16b3becfc12ed22a576741a505080e9dc279d4ab54d8c8ef597b729efb3c41e`。

此前补充实验使用的 `/root/data3/huangchanni/data/from_nj.csv`、`/root/data3/huangchanni/data/to_nj.csv` 与项目内 `data/` 文件哈希完全一致，因此已有 RAMR 消融和现代强基线结果仍可复核。为避免后续误读外部目录，已将训练、评估、绘图和现代基线脚本的默认数据目录统一为项目内 `data/`。

本轮又显式使用 `--data_dir data` 重新评估 RAMR 消融 checkpoint，并重新训练 DLinear、PatchTST、iTransformer、TimeMixer、FreEformer 五个现代强基线。复现实验日志保存在 `../logs/paper_experiments_correct_data/`，现代强基线结果保存在 `../results/modern_baselines_correct_data/`。因此论文中的补充实验数值均可追溯到项目内真实 CSV。

为进一步避免后续复现实验误用测试集，本轮还加固了旧脚本入口：`src/eval_all_checkpoints.py` 默认只读取 checkpoint 中保存的验证集指标并按验证集 MAPE 排名，不访问测试集；旧的测试集遍历模式仅保留为 legacy/debug，必须显式传入 `--allow_test_selection`。`src/pick_best_test_checkpoint.py` 默认拒绝运行，并提示“按测试集选择 checkpoint 会造成测试集泄露”。训练过程中新保存的过程快照目录改为 `snapshots_for_diagnostics`，不再使用“测试选模快照”表述。

## 专家意见1

### 1. 统计特征能否分析突发事件、极端天气等不确定因素

已修改。修订稿明确承认：仅依靠均值、标准差、最大值不能识别突发事件或极端天气的具体成因，也不能替代天气、活动、运营管制等外生变量。

对应修改：

- 将原“统计特征专家”升级为“Robust Distribution Shift Expert（分布偏移专家）”。
- 新增 robust z-score、quantile skewness、peak-to-average ratio、volatility index 等窗口级分布偏移特征。
- 论文定位改为：统计/鲁棒特征刻画 rare-event 下的 distribution shift，而非直接判断事件类型。

补充实验结果如实写入论文：basic（mean/std/max）测试 MAPE 为 19.74%，quantile 为 19.96%，robust/RAMR 为 20.88%。因此修订稿不声称鲁棒统计必然带来最低误差，而是强调其提升机制解释性，并建议后续接入天气、活动日历、列车开行调整等可预报外生变量。

### 2. 2.1 数据集介绍增加表格

已在 `2.1 数据集描述` 补充两个表格，并确保表格值来自 `data/from_nj.csv` 与 `data/to_nj.csv` 的真实前五行：

- “表1-0 数据集字段说明”：列出 passenger flow、date、weekday、holiday、weekend/workday adjustment、trend/regime statistics 等字段、类型和说明。
- “表1-1 数据集样例”：列出部分真实样例字段，包括 `time`、`北京南_from_nj`、`成都东_from_nj`、`北京南_to_nj`、`成都东_to_nj`。

同时保留数据规模说明：from 表 854 条、to 表 852 条，按日期内连接后得到 846 个有效共同日期、20 个双向客流变量。

### 3. 未说明特征工程及特征选择

已新增 `2.1.1 多视图特征工程与特征选择`。

修订稿将特征空间拆分为四类：

- 历史客流窗口：20 个 from/to 方向客流序列，RobustScaler 归一化。
- 日历场景特征：星期、月份、周末、节假日、调休工作日、日期归一化及周期编码。
- 滚动统计特征：窗口均值、标准差、最大值、分位数、分布偏态。
- 鲁棒扰动特征：robust z-score、quantile skewness、peak-to-average ratio、volatility index。

特征选择原则已写入论文：预测时可获得、不引入未来信息、与短期依赖、长期周期、分布扰动三类专家功能对齐。

### 4. 短期专家权重偏低如何解释

已重新核验 `2.7` 门控权重。重算发现旧版表格存在专家列顺序解释问题：当前模型输出专家顺序为 `ShortTerm / LongTerm / DistributionShift`，因此不能继续按“长期、短期、分布偏移”的旧列顺序解释。修订稿已据此替换表4、表5和对应文字。

严格测试集门控统计为：

- 工作日样本 152 个：短期专家 0.5927，长期专家 0.0845，分布偏移专家 0.3228。
- 周末样本 60 个：短期专家 0.2196，长期专家 0.2011，分布偏移专家 0.5793。
- 当前严格时间顺序测试窗口节假日样本数为 0，因此不再报告节假日 vs 工作日显著性检验，也不声称当前测试集上节假日权重抬升显著。

修订后的解释为：短期专家并非在所有场景中权重最低。工作日中短期专家权重最高，说明其承担相邻日期连续性和短程校正；周末中分布偏移专家权重显著升高，说明非工作日需求结构变化触发分布偏移建模。Welch t 检验显示，周末分布偏移专家权重显著高于工作日（0.5793 vs 0.3228，t=4.9757，p=2.42e-06）。该结论比旧版“短期专家整体低权重”的解释更符合当前模型真实路由行为。

同时保留波动率-短期权重图作为补充可视化。该图在 `北京南_from_nj` 上 Pearson r=0.0467、p=0.4990，未形成显著线性相关。修订稿据此避免过度声称“波动越大短期权重必然越高”，机制证据以场景门控均值、周末/工作日 Welch 检验和专家消融为主。

### 5. 泛化实验从铁路日粒度到 Traffic 小时级跨度过大

已重写 `2.8 泛化性实验`。

修订稿明确采用 domain adaptation framing：

- 不主张铁路日粒度模型可直接跨分辨率迁移到小时级道路交通。
- Traffic 实验仅用于评估专家分解结构在重构时间窗口、周期特征和扰动代理后的结构鲁棒性。
- 迁移到小时级数据时必须重新定义小时级历史窗口、预测窗口、小时/星期周期编码和局部波动统计，不能直接复用日粒度配置。

结论从过强跨域表述降级为“不同时间粒度下专家分解结构具有初步适应性，稳定泛化仍需更多数据验证”。

### 6. 摘要不要出现公式

已修改。中文摘要和英文摘要均删除公式表达，改为文字描述“趋势、周期和残差扰动交织的多尺度非平稳特征”以及“trend, seasonal, and residual disturbance components”。

### 7. VAR 表现差，建议重跑或更换对比方法

已补充现代强基线，并在铁路主数据集上重新训练：

| 模型 | 测试MAPE/% | MSE | MAE |
| --- | ---: | ---: | ---: |
| DLinear | 24.34 | 0.054314 | 0.182450 |
| PatchTST | 24.31 | 0.054858 | 0.170963 |
| iTransformer | 21.37 | 0.043506 | 0.154333 |
| TimeMixer | 23.55 | 0.042384 | 0.159879 |
| FreEformer | 20.15 | 0.040026 | 0.148139 |
| MoE-Rail 原模型 | 19.41 | 0.037700 | 0.144000 |

修订稿将 VAR 作为传统基线保留但不再依赖其弱结果支撑优势，重点补充 FreEformer、iTransformer、PatchTST、TimeMixer、DLinear 等现代强基线。

### 8. 英文字体不统一

已用脚本统一 Word 文档字体：

- 英文与数字：Times New Roman。
- 中文：宋体。
- 表格内容同步处理。

### 9. 图4第一张图右侧不清楚

已补充拆分图和机制解释图，生成位置：

- `../results/paper_figures_ramr_full/图_STL分解与门控权重对照_stl.png`
- `../results/paper_figures_ramr_full/图_STL分解与门控权重对照.png`
- `../results/paper_figures_ramr_full/图_场景门控权重箱线图.png`
- `../results/paper_figures_ramr_full/图_波动率与短期专家权重关系.png`

Word 稿中已插入门控权重箱线图、STL 对照图和波动率-短期专家权重图，避免仅依赖原图4的单一拥挤视图。

### 10. 2.5 节 St、Tt、Rt 没写下标

已统一改为 `Sₜ`、`Tₜ`、`Rₜ`。同时检查并修复了误替换导致参考文献中 “Statistical” 被改写的问题。

## 专家意见2

### 1. 创新性不足，像已有模块组合应用

已从论文主叙事层面修改。修订稿不再把贡献写成“GRU + Transformer + MLP 的组合”，而是提出：

- 场景条件化非平稳分解-路由机制；
- Regime-aware Mixture Routing（RAMR）；
- Data decomposition、Regime encoding、Expert specialization 三层结构解释。

新增 Regime Encoder：以 volatility、trend strength、seasonality strength 等窗口级状态构造 regime embedding，再输入门控网络，使论文核心从“模型拼接”转为“distribution regime 到 expert routing 的显式映射”。

论文引言已进一步改成顶会式贡献表达：先给出场景条件化非平稳分解-路由的总体机制，再列出分布偏移专家、regime-aware routing、真实铁路双向客流验证和强基线/机制分析四类贡献，突出方法论归纳偏置而非工程堆叠。

### 2. 需要与 PatchTST、TimeMixer、iTransformer、FreEformer 等更新方法对比

已完成铁路主数据集补充实验，并在 `2.5` 新增“表1-3 现代强基线补充实验结果（铁路主数据集）”和“图2-1 现代强基线在铁路主数据集上的 MAPE 对比”。

新增脚本：

- `src/train_modern_baselines.py`

实验日志与结果：

- `../logs/paper_experiments/modern_baselines.log`
- `../results/modern_baselines/modern_baselines_results.csv`
- `../results/modern_baselines/modern_baselines_mape.png`
- 正确数据路径复现实验：`../logs/paper_experiments_correct_data/modern_baselines.log`
- 正确数据路径结果：`../results/modern_baselines_correct_data/modern_baselines_results.csv`

### 3. MSE 好但 MAE 一般，跨域泛化结论不充分

已重新实验并修改。除原有 RAMR 消融和现代强基线外，新增了 MAE-aware multi-objective 训练，并进一步补充严格无泄露验证集集成（Strict RAMR-VE）。本轮先修正数据预处理：`RobustScaler` 默认仅使用训练集拟合，再变换验证集和测试集，避免验证/测试分布泄露到训练预处理。随后在验证集上确定 checkpoint 集合、集成权重和尺度校准参数；候选表只导出验证集指标，不导出候选测试指标；测试集只在最终方案固定后评估一次。最终结果文件为 `../results/strict_no_leakage_ensemble/all_balanced_final_once/ensemble_summary.json`。

| 模型 | 测试MAPE/% | MSE | MAE |
| --- | ---: | ---: | ---: |
| 原RAMR完整模型 | 20.88 | 0.043960 | 0.151176 |
| RAMR-MAE-basic | 20.33 | 0.039290 | 0.149355 |
| RAMR-MAE-basic-mape | 20.34 | 0.040598 | 0.148552 |
| MoE-Rail原模型 | 19.41 | 0.037700 | 0.144000 |
| FreEformer | 20.15 | 0.040026 | 0.148139 |
| Strict RAMR-VE（验证集选权重+尺度） | **18.55** | **0.035640** | **0.138028** |

实验结论更新为：单一 MAE 权重调整可以缓解 RAMR 的 MAE 弱项，但仍存在 MAPE/MSE/MAE 权衡；进一步引入验证集集成后，Strict RAMR-VE 在严格无测试泄露口径下取得 MAPE=18.55%、MSE=0.035640、MAE=0.138028，相比原 MoE-Rail 的 19.41%、0.037700、0.144000 三项指标均改善。该改进不是测试集调参，而是由验证集 balanced objective 选出，测试集只用于最终一次评估。

论文表述已同步修改为：

- Traffic 结果用于说明结构鲁棒性初步成立，不证明日粒度铁路模型可直接跨分辨率迁移。
- 铁路主数据集最终主结果采用 Strict RAMR-VE，可写成 MAPE/MSE/MAE 三项指标均优于旧 MoE-Rail 与现代强基线。
- 结论中增加限制：稳定泛化仍需更多铁路线路、更多公开数据集和更多外生扰动变量验证。

### 4. 消融实验建议增加日历特征输入、负载均衡约束

已补充可复现实验：

| 消融项 | 测试MAPE/% | 说明 |
| --- | ---: | --- |
| RAMR 完整模型（scene+regime+robust） | 20.88 | 完整机制 |
| 无 regime 路由 | 21.37 | 移除窗口级非平稳状态编码 |
| 无场景门控 | 22.44 | 移除日历场景条件 |
| 方差负载均衡 | 20.91 | 与默认高熵负载均衡约束接近 |

结果显示：无场景门控带来 1.56 个百分点退化，说明日历场景输入对路由有效；无 regime 路由带来 0.49 个百分点退化，说明窗口级状态编码有贡献；负载均衡形式影响较小但有助于稳定专家利用。

### 5. 表述不清：日粒度论文是否应提“日内高频/早晚高峰”；熵正则与公式不一致

已修改：

- 删除或改写“日内高频波动”“早晚高峰”“瞬时波动”等不适合日粒度预测的表述。
- 统一改为“相邻日期局部波动”“跨周周期”“节假日前后迁移”“窗口级分布偏移”。
- 将“熵正则”统一为“高熵负载均衡约束”，公式与代码实现保持一致：主实验最小化\(\sum_k \bar{g}_k\log(\bar{g}_k+\epsilon)\)，等价于最大化批次平均专家使用熵；方差型约束仅作为消融对照。

## 本轮新增实验与图表清单

- RAMR 关键消融：scene、regime、统计特征集合、负载均衡形式。
- 现代强基线：DLinear、PatchTST、iTransformer、TimeMixer、FreEformer。
- 严格无泄露改进实验：训练集拟合 scaler，验证集选择集成权重和尺度，测试集最终一次评估，结果见 `docs/experiments/严格无泄露改进实验_20260630.md`。
- 复现脚本防泄露加固：`eval_all_checkpoints.py` 默认验证集排名，legacy 测试集扫描需显式确认；训练快照改为 diagnostics 用途。
- 门控解释图：场景门控箱线图、STL 分解与门控权重对照图、波动率-短期专家权重散点图。
- 数据集表格：字段说明表、部分真实样例表。
- 特征工程表：多视图特征工程与专家/路由作用对应表。

## 需要在回复审稿人时保留的边界

- 不能声称窗口统计特征可以识别极端天气或具体突发事件，只能刻画已反映在客流中的分布偏移。
- 可以声明 Strict RAMR-VE 在当前南京南双向铁路主数据集上三项指标均优于旧 MoE-Rail，但不能推广为任意线路、任意时间粒度都必然优越。
- 不能声称 Traffic 小时级实验证明日粒度铁路方法可直接跨分辨率迁移；只能写作重构特征后的结构鲁棒性验证。
