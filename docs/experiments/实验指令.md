# 回看窗口（Lookback）消融实验 - 执行指令

## 📋 实验说明

**目标**：测试不同的 lookback 值（6, 7, 10, 14, 30），找出最适合的历史数据长度。

**重要**：
- `summarize_lookback_results.py` **不需要重新训练**，只是汇总已有结果的脚本
- **但要测试不同 lookback 值，需要重新训练每个值的模型**

## ✅ 当前状态

- Lookback=6: ✅ 已训练完成
- Lookback=7, 10, 14, 30: ❌ 未训练

## 🚀 执行步骤

### 步骤1：检查当前状态

```bash
cd /root/data3/huangchanni/moe/moe_nanjin1

# 检查已训练的模型
ls -lh ../checkpoints/moe_nanjin_lookback_*/

# 检查是否有评估结果
ls -lh ../results/moe_nanjin_lookback_ablation/ 2>/dev/null
```

### 步骤2：训练缺失的 lookback 值

#### 方法A：使用实验脚本（自动训练所有值）

```bash
cd /root/data3/huangchanni/moe/moe_nanjin1

# 后台运行完整实验
nohup bash train_lookback_ablation.sh > lookback_ablation.log 2>&1 &

# 查看实时日志
tail -f lookback_ablation.log

# 或查看最后50行
tail -50 lookback_ablation.log
```

#### 方法B：单独训练每个缺失的值

```bash
cd /root/data3/huangchanni/moe/moe_nanjin1

# 训练 lookback=7
python3 train.py \
    --data_dir ../data \
    --lookback 7 \
    --horizon 1 \
    --batch_size 64 \
    --hidden_dim 512 \
    --dropout 0.15 \
    --num_epochs 300 \
    --patience 50 \
    --learning_rate 0.0008 \
    --weight_decay 0.0001 \
    --lambda_mape 0.5 \
    --lambda_mae 1.0 \
    --lambda_mse 0.5 \
    --lambda_balance 0.01 \
    --lambda_range 0.1 \
    --use_both \
    --num_workers 8 \
    --gpu_ids 3,4,5 \
    --ablation_mode baseline \
    --save_dir ../checkpoints/moe_nanjin_lookback_7

# 训练 lookback=10
python3 train.py \
    --data_dir ../data \
    --lookback 10 \
    --horizon 1 \
    --batch_size 64 \
    --hidden_dim 512 \
    --dropout 0.15 \
    --num_epochs 300 \
    --patience 50 \
    --learning_rate 0.0008 \
    --weight_decay 0.0001 \
    --lambda_mape 0.5 \
    --lambda_mae 1.0 \
    --lambda_mse 0.5 \
    --lambda_balance 0.01 \
    --lambda_range 0.1 \
    --use_both \
    --num_workers 8 \
    --gpu_ids 3,4,5 \
    --ablation_mode baseline \
    --save_dir ../checkpoints/moe_nanjin_lookback_10

# 训练 lookback=14
python3 train.py \
    --data_dir ../data \
    --lookback 14 \
    --horizon 1 \
    --batch_size 64 \
    --hidden_dim 512 \
    --dropout 0.15 \
    --num_epochs 300 \
    --patience 50 \
    --learning_rate 0.0008 \
    --weight_decay 0.0001 \
    --lambda_mape 0.5 \
    --lambda_mae 1.0 \
    --lambda_mse 0.5 \
    --lambda_balance 0.01 \
    --lambda_range 0.1 \
    --use_both \
    --num_workers 8 \
    --gpu_ids 3,4,5 \
    --ablation_mode baseline \
    --save_dir ../checkpoints/moe_nanjin_lookback_14

# 训练 lookback=30
python3 train.py \
    --data_dir ../data \
    --lookback 30 \
    --horizon 1 \
    --batch_size 64 \
    --hidden_dim 512 \
    --dropout 0.15 \
    --num_epochs 300 \
    --patience 50 \
    --learning_rate 0.0008 \
    --weight_decay 0.0001 \
    --lambda_mape 0.5 \
    --lambda_mae 1.0 \
    --lambda_mse 0.5 \
    --lambda_balance 0.01 \
    --lambda_range 0.1 \
    --use_both \
    --num_workers 8 \
    --gpu_ids 3,4,5 \
    --ablation_mode baseline \
    --save_dir ../checkpoints/moe_nanjin_lookback_30
```

### 步骤3：评估所有模型

```bash
cd /root/data3/huangchanni/moe/moe_nanjin1

# 创建结果目录
mkdir -p ../results/moe_nanjin_lookback_ablation

# 评估每个 lookback 值
for lookback in 6 7 10 14 30; do
    echo "=========================================="
    echo "评估 Lookback = $lookback"
    echo "=========================================="
    python3 evaluate.py \
        --checkpoint ../checkpoints/moe_nanjin_lookback_${lookback}/best_model_latest.pth \
        --data_dir ../data \
        --batch_size 64 \
        --num_workers 8 > ../results/moe_nanjin_lookback_ablation/lookback_${lookback}_evaluation.txt 2>&1
    echo "完成 Lookback = $lookback"
    echo ""
done
```

### 步骤4：汇总结果

```bash
cd /root/data3/huangchanni/moe/moe_nanjin1
python3 summarize_lookback_results.py
```

查看汇总报告：
```bash
cat ../results/moe_nanjin_lookback_ablation/lookback_ablation_summary.txt
```

## 📊 结果位置

- **模型checkpoints**：`../checkpoints/moe_nanjin_lookback_{lookback}/`
- **评估结果**：`../results/moe_nanjin_lookback_ablation/lookback_{lookback}_evaluation.txt`
- **汇总报告**：`../results/moe_nanjin_lookback_ablation/lookback_ablation_summary.txt`

## ⏱️ 预计时间

- 每个 lookback 值：2-4小时
- 5个值总计：10-20小时
- 评估时间：每个约5-10分钟

## 🔍 监控训练进度

```bash
# 查看训练日志
tail -f lookback_ablation.log

# 查看GPU使用情况
nvidia-smi

# 检查训练进程
ps aux | grep train.py | grep lookback
```

## ⚠️ 注意事项

1. 确保GPU 3,4,5可用（避免与Timer-XL任务冲突）
2. lookback=30需要更多历史数据，训练样本数会减少
3. 所有实验使用相同随机种子（42）确保可复现性







